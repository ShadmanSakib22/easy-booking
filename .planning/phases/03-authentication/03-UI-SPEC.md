---
phase: 3
slug: authentication
status: approved
shadcn_initialized: true
preset: base-nova
created: 2026-06-17
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (CLI, base-nova preset) — initialized this session via `npx shadcn@latest init --defaults` |
| Preset | `base-nova` (radix-style neutral grayscale theme, Base UI primitives, oklch color tokens) — shadcn default for Next.js, applied since CONTEXT.md/RESEARCH.md gave no brand-color direction for this phase and the user authorized "industry-standard defaults on remaining unresolved gray areas" |
| Component library | Base UI (`@base-ui/react`) — note: shadcn's current default registry uses Base UI primitives, not Radix UI, despite CLAUDE.md referencing "Radix UI primitives." Functionally equivalent (unstyled, accessible primitives); CLAUDE.md's wording is stale, not a blocker. |
| Icon library | lucide-react ^1.20.0 (installed automatically by shadcn init; matches CONTEXT.md D-13) |
| Font | Geist Sans (var `--font-sans`, already configured in `app/layout.tsx` via `next/font/google`) for body/UI text; Geist Mono retained for any code/monospace needs (unused in this phase) |

Installed components (this session): `button`, `input`, `card`, `label`, `alert`, `skeleton`. (`form` is not a separate shadcn registry item in this preset — react-hook-form wiring is done manually against `input`/`label`/`alert` per CONTEXT.md D-12, matching RESEARCH.md's code examples.)

**Required override:** `components/ui/button.tsx` ships with `font-medium` (500) on its base class string by default. This phase requires changing that to `font-semibold` (600) before implementing any auth screens, so button labels align with the Heading/Display weight and the project's typography contract holds at exactly two weights (400, 600) with zero exceptions. Executor must apply this override as the first task in this phase, then verify no other component re-introduces `font-medium`/500.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps inside buttons/inputs, inline padding |
| sm | 8px | Compact stacking (label → input gap, helper text gap) |
| md | 16px | Default form-field vertical spacing, card internal padding (mobile) |
| lg | 24px | Card internal padding (desktop), spacing between form and footer links |
| xl | 32px | Vertical gap between `AuthLayout`'s logo/heading block and the card |
| 2xl | 48px | Top/bottom page padding on `AuthLayout` at desktop viewport |
| 3xl | 64px | Not used in this phase (no full-page marketing sections) |

Exceptions:
- Icon-only buttons (Google "G" glyph button, password show/hide toggle) use a 32px (`size-8`, shadcn default `icon` button size) square touch target — below the 44px mobile-accessibility guideline but consistent with shadcn's dense default scale used throughout; acceptable for desktop-first auth flows. **Manual QA checklist item (required before phase sign-off):** on a real mobile viewport (or device emulation at 375px width), tap-test both the Google "G" glyph button and the password show/hide toggle; confirm they are comfortably tappable with a finger. If not, bump to `size-9` (36px) or wrap with extra hit-area padding — do not ship at 32px without this check having been performed and recorded.
- Skeleton loading block (`Guard.tsx` loading state) uses `md` (16px) internal gaps between skeleton lines, not a spacing scale violation, just noting non-content nature.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`text-sm`) | 400 (regular) | 1.5 |
| Label | 14px (`text-sm`) | 600 (semibold) | 1.4 |
| Heading | 20px (`text-xl`) | 600 (semibold) | 1.2 |
| Display | 28px (`text-3xl`... use `text-[1.75rem]`/`text-3xl`) | 600 (semibold) | 1.2 |

Notes:
- Exactly two weights, no exceptions: 400 (regular — body copy, helper/error text) and 600 (semibold — labels, headings, card titles, button labels, "Sign in" / "Create account" page titles). `components/ui/button.tsx`'s default `font-medium` (500) is overridden to `font-semibold` (600) per the Design System section above, so button labels carry the same weight as headings/labels — no third weight value exists anywhere in this contract.
- Display (28px/600) is reserved for the single page-level heading at the top of each `AuthLayout` card (e.g. "Welcome back", "Create your account", "Reset your password"). Heading (20px/600) is reserved for sub-sections (e.g. card title inside `CardHeader` if distinct from the page-level heading — in practice these will usually collapse into one element; do not stack both on the same page).
- Body line-height 1.5 (form helper text, error messages, empty-state copy). Heading/Display line-height 1.2.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `oklch(1 0 0)` / `--background` (white) light mode, `oklch(0.145 0 0)` dark mode | Page background behind `AuthLayout`, body text contrast `--foreground` |
| Secondary (30%) | `oklch(1 0 0)` / `--card` (white, light) — visually distinct from background only via `--border` (`oklch(0.922 0 0)`) and subtle shadow | The centered auth card itself (`Card` component), and `--muted` (`oklch(0.97 0 0)`) for input backgrounds/disabled states |
| Accent (10%) | `--primary` = `oklch(0.205 0 0)` (near-black in light mode) | Reserved EXCLUSIVELY for: primary CTA buttons ("Create account", "Sign in", "Send reset link", "Save password"), active focus ring (`--ring`), and links inside body copy (e.g. "Forgot password?", "Already have an account? Sign in") |
| Destructive | `--destructive` = `oklch(0.577 0.245 27.325)` (red) | Reserved EXCLUSIVELY for: inline form validation errors, the centralized Firebase auth-error `Alert` banner (D-14), and the password-change re-authentication failure state — never used for non-error emphasis |

Accent reserved for: primary submit buttons on each auth form, the focus ring on the currently-focused input, and inline text links connecting auth pages to each other (sign-in ↔ sign-up, forgot-password back-link). Google sign-in button uses `outline` button variant (neutral border, not accent) so it visually reads as a secondary action relative to the primary email/password CTA — this satisfies the "one obvious default action per page" rule.

This is a grayscale-dominant palette by design (shadcn `base-nova` neutral preset, no project brand color decided yet) — 60/30/10 here refers to surface/elevation hierarchy (background vs. card vs. interactive-accent), not hue variety. If a brand color is chosen in a future phase, only the `--primary`/accent role should be re-themed; do not introduce a second accent.

**Focal point per page type:**
- Sign-in / sign-up card pages: the focal point is the primary submit button (`Sign in` / `Create account`) — it is the only `--primary`-filled element on the page, positioned directly below the form fields, drawing the eye via color contrast against the otherwise grayscale card.
- Verify-email pending state: the focal point is the "Check your email" Display heading paired with the email-icon visual immediately above it — there is no form on this screen, so hierarchy is carried by typographic weight (28px/600) rather than color, with the "Resend email" link/button as the secondary focal element below.

**Icon-only control accessibility:** Both icon-only interactive controls in this phase must carry an explicit `aria-label` (not just a visual icon) — the Google sign-in button's icon variant (if used standalone) takes `aria-label="Continue with Google"`, and the password show/hide toggle takes `aria-label="Show password"` when masked and `aria-label="Hide password"` when revealed (label text swaps with state, not a static label).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — Sign up | "Create account" |
| Primary CTA — Sign in | "Sign in" |
| Primary CTA — Forgot password | "Send reset link" |
| Primary CTA — Change password | "Update password" |
| Secondary CTA — Google | "Continue with Google" |
| Empty state heading | Not applicable — auth pages have no data-driven empty states (forms always render); `/account/security` page itself has no "empty" condition |
| Empty state body | N/A (see above) |
| Verify-email pending state heading | "Check your email" |
| Verify-email pending state body | "We sent a verification link to {email}. Click the link to verify your account, then return here." + resend action: "Resend email" (disabled with countdown for 60s after each send, per RESEARCH.md Open Question §3 recommendation) |
| Error state — generic auth failure | Centralized message from `lib/firebase/auth-errors.ts` (D-14), e.g. "Incorrect email or password." shown in an `Alert variant="destructive"` directly above the submit button — never a raw Firebase error string |
| Error state — network failure | "Network error. Please check your connection and try again." (already in RESEARCH.md's error map) |
| Error state — re-auth required (password change) | "For security, please sign in again before changing your password." followed by a re-auth prompt (current-password field) inline on the same page, not a separate route |
| Destructive confirmation — none in this phase | No delete/destroy actions exist in Phase 3 (account deletion is out of scope; password change is a mutation, not destructive-confirm-worthy). N/A. |
| Onboarding username step heading | "Choose a username" |
| Onboarding username step body | "This will be your public profile URL: easyappointment.app/u/{username}" (live preview as user types) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official (`base-nova` preset, default registry) | `button`, `input`, `card`, `label`, `alert`, `skeleton` | not required (first-party registry) |
| third-party | none | not applicable — no third-party registries declared for this phase |

No third-party registry blocks were requested or installed. All components above came from shadcn's first-party default registry during `npx shadcn@latest init --defaults` and `npx shadcn@latest add input card label alert skeleton -y`.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-06-17 by gsd-ui-checker (1 revision: typography weight violation fixed in commit ed7c0d6)
