---
phase: 02-firebase-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - my-app/package.json
  - my-app/vitest.config.ts
  - firebase.json
  - firestore.rules
  - firestore.indexes.json
  - storage.rules
  - .firebaserc
autonomous: true
requirements: []

must_haves:
  truths:
    - "firebase-tools CLI is available in PATH (`firebase --version` exits 0)"
    - "Java JDK 11+ is installed (`java -version` exits 0)"
    - "firebase, firebase-admin, @firebase/rules-unit-testing, vitest, concurrently, tsx, server-only are present in my-app/package.json"
    - "vitest.config.ts exists in my-app/ with correct test include path"
    - "firebase.json exists at repo root with Auth (9099), Firestore (8080), Storage (9199) emulator ports"
    - "firestore.rules stub exists with rules_version = '2' and deny-all defaults"
    - "firestore.indexes.json exists with the 6 composite index entries"
    - "storage.rules stub exists"
    - ".firebaserc exists referencing demo-easyappointment"
    - ".env.local.example exists with all required env var names"
  artifacts:
    - path: "my-app/package.json"
      provides: "Updated dependencies and scripts"
    - path: "my-app/vitest.config.ts"
      provides: "Test runner config pointing at lib/firebase/__tests__"
    - path: "firebase.json"
      provides: "Emulator port configuration"
    - path: "firestore.rules"
      provides: "Security rules file (stub — full rules in Plan 03)"
    - path: "firestore.indexes.json"
      provides: "6 composite indexes"
    - path: "storage.rules"
      provides: "Storage rules stub"
    - path: ".firebaserc"
      provides: "Firebase project alias"
    - path: "my-app/.env.local.example"
      provides: "Environment variable reference template"
  key_links:
    - from: "my-app/vitest.config.ts"
      to: "my-app/lib/firebase/__tests__/*.test.ts"
      via: "include glob"
      pattern: "lib/firebase/__tests__"
    - from: "firebase.json"
      to: "firestore.rules"
      via: "firestore.rules key"
      pattern: "\"rules\": \"firestore.rules\""
---

<objective>
Install all Phase 2 prerequisites and scaffold the Firebase project configuration files so that Waves 2 and 3 can proceed without environment blockers.

Purpose: Nothing in Phase 2 can run without Java (emulators), firebase-tools (CLI), npm packages, and firebase.json. This wave-1 plan removes every blocker before SDK and rules work begins.
Output: Installed toolchain, vitest config, firebase.json, stub rules files, composite indexes, env example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-firebase-foundation/02-CONTEXT.md
@.planning/phases/02-firebase-foundation/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Java JDK 21, firebase-tools, and all npm packages</name>
  <files>my-app/package.json</files>
  <read_first>
    - my-app/package.json (current dependencies before modification)
  </read_first>
  <action>
Step 1 — Verify Java. Run `java -version`. If Java is not found or the version is below 11, install JDK 21:
```
sudo apt-get update && sudo apt-get install -y openjdk-21-jdk
```
Verify: `java -version` must output a version string containing "21" or higher.

Step 2 — Install firebase-tools globally:
```
npm install -g firebase-tools
```
Verify: `firebase --version` exits 0.

Step 3 — From the `my-app/` directory, add production npm packages:
```
cd /home/toasty/Desktop/Code\ Projects/EasyAppointment/my-app
pnpm add firebase@^12 firebase-admin@^14 server-only
```

Step 4 — From `my-app/`, add dev dependencies:
```
pnpm add -D @firebase/rules-unit-testing@^5 vitest@^4 concurrently@^10 tsx@^4
```

Step 5 — Verify package.json now contains the following entries under `dependencies`:
- `"firebase"`: version starting with `^12`
- `"firebase-admin"`: version starting with `^14`
- `"server-only"`: any version

And under `devDependencies`:
- `"@firebase/rules-unit-testing"`: version starting with `^5`
- `"vitest"`: any version
- `"concurrently"`: any version
- `"tsx"`: any version
  </action>
  <verify>
    <automated>cd /home/toasty/Desktop/Code\ Projects/EasyAppointment/my-app && node -e "const p = require('./package.json'); const deps = {...p.dependencies,...p.devDependencies}; ['firebase','firebase-admin','server-only','@firebase/rules-unit-testing','vitest','concurrently','tsx'].forEach(d => { if (!deps[d]) throw new Error('Missing: ' + d); }); console.log('All packages present');" && firebase --version && java -version</automated>
  </verify>
  <done>All 7 packages present in package.json; `firebase --version` and `java -version` exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Firebase project files and vitest config</name>
  <files>
    firebase.json
    firestore.rules
    firestore.indexes.json
    storage.rules
    .firebaserc
    my-app/vitest.config.ts
    my-app/.env.local.example
    my-app/package.json
  </files>
  <read_first>
    - my-app/package.json (need to update scripts section)
    - my-app/tsconfig.json (check paths and module settings for vitest config)
  </read_first>
  <action>
All files are created from the repo root `/home/toasty/Desktop/Code Projects/EasyAppointment/`.

**1. Create `firebase.json` at repo root:**
```json
{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": {
    "rules": "storage.rules"
  },
  "emulators": {
    "auth": {
      "port": 9099
    },
    "firestore": {
      "port": 8080
    },
    "storage": {
      "port": 9199
    },
    "ui": {
      "enabled": true,
      "port": 4000
    }
  }
}
```

**2. Create `.firebaserc` at repo root:**
```json
{
  "projects": {
    "default": "demo-easyappointment"
  }
}
```

**3. Create `firestore.rules` at repo root (deny-all stub — replaced fully in Plan 03):**
```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

**4. Create `firestore.indexes.json` at repo root with all 6 composite indexes:**
```json
{
  "indexes": [
    {
      "collectionGroup": "slots",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "calendarId", "order": "ASCENDING" },
        { "fieldPath": "status", "order": "ASCENDING" },
        { "fieldPath": "startUtc", "order": "ASCENDING" }
      ]
    },
    {
      "collectionGroup": "slots",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "calendarId", "order": "ASCENDING" },
        { "fieldPath": "startUtc", "order": "ASCENDING" }
      ]
    },
    {
      "collectionGroup": "slots",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "calendarId", "order": "ASCENDING" },
        { "fieldPath": "date", "order": "ASCENDING" }
      ]
    },
    {
      "collectionGroup": "bookings",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "creatorId", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "bookings",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "creatorId", "order": "ASCENDING" },
        { "fieldPath": "calendarId", "order": "ASCENDING" }
      ]
    },
    {
      "collectionGroup": "bookings",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "bookerUid", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
```

**5. Create `storage.rules` at repo root:**
```
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
```

**6. Create `my-app/vitest.config.ts`:**
```typescript
import { defineConfig } from 'vitest/config'
import path from 'path'

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['lib/firebase/__tests__/**/*.test.ts'],
    testTimeout: 30000,
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './'),
    },
  },
})
```

**7. Create `my-app/.env.local.example`:**
```bash
# Firebase client config — safe to be public; scoped by security rules
NEXT_PUBLIC_FIREBASE_API_KEY=AIza...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abc123

# Emulator toggle — set to "true" for local dev; absent or "false" for production
NEXT_PUBLIC_USE_FIREBASE_EMULATOR=true

# Firebase Admin credentials — server-only secrets (no NEXT_PUBLIC_ prefix)
# Private key must have literal \n for newlines (auto-unescaped in admin.ts)
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----\n"
```

**8. Update `my-app/package.json` scripts section** — merge these scripts into the existing `"scripts"` object (keep `build`, `start`, `lint`):
```json
{
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"EMU,NEXT\" --prefix-colors \"yellow,cyan\" \"cd .. && firebase emulators:start --project demo-easyappointment\" \"next dev\"",
    "dev:next": "next dev",
    "emulators": "cd .. && firebase emulators:start --project demo-easyappointment",
    "seed": "FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099 tsx scripts/seed-emulator.ts",
    "test:rules": "vitest run lib/firebase/__tests__/firestore.rules.test.ts",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  }
}
```
  </action>
  <verify>
    <automated>test -f /home/toasty/Desktop/Code\ Projects/EasyAppointment/firebase.json &amp;&amp; test -f /home/toasty/Desktop/Code\ Projects/EasyAppointment/firestore.rules &amp;&amp; test -f /home/toasty/Desktop/Code\ Projects/EasyAppointment/firestore.indexes.json &amp;&amp; test -f /home/toasty/Desktop/Code\ Projects/EasyAppointment/storage.rules &amp;&amp; test -f /home/toasty/Desktop/Code\ Projects/EasyAppointment/.firebaserc &amp;&amp; test -f /home/toasty/Desktop/Code\ Projects/EasyAppointment/my-app/vitest.config.ts &amp;&amp; test -f /home/toasty/Desktop/Code\ Projects/EasyAppointment/my-app/.env.local.example &amp;&amp; grep -q "rules_version = '2'" /home/toasty/Desktop/Code\ Projects/EasyAppointment/firestore.rules &amp;&amp; grep -q "9099" /home/toasty/Desktop/Code\ Projects/EasyAppointment/firebase.json &amp;&amp; grep -q "8080" /home/toasty/Desktop/Code\ Projects/EasyAppointment/firebase.json &amp;&amp; grep -q "9199" /home/toasty/Desktop/Code\ Projects/EasyAppointment/firebase.json &amp;&amp; python3 -c "import json; d=json.load(open('/home/toasty/Desktop/Code Projects/EasyAppointment/firestore.indexes.json')); assert len(d['indexes'])==6, f'Expected 6 indexes, got {len(d[\"indexes\"])}'; print('6 indexes OK')" &amp;&amp; grep -q "concurrently" /home/toasty/Desktop/Code\ Projects/EasyAppointment/my-app/package.json &amp;&amp; grep -q "test:rules" /home/toasty/Desktop/Code\ Projects/EasyAppointment/my-app/package.json &amp;&amp; echo "All scaffold files OK"</automated>
  </verify>
  <done>All 8 files exist; firebase.json contains correct ports; firestore.indexes.json has 6 entries; firestore.rules begins with rules_version = '2'; package.json has concurrently dev script and test:rules script; .env.local.example has all 9 env var names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | .env.local.example contains no real secrets; actual .env.local is gitignored |
| npm global → system | firebase-tools installed globally; must be from official npm registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01-01 | Information Disclosure | `.env.local` | mitigate | `.env.local` must be in `.gitignore` (verify in Task 2: `grep -r ".env.local" .gitignore`) |
| T-2-01-02 | Tampering | `firebase.json` emulator ports | accept | Emulator-only; no production traffic; port conflicts would be surfaced immediately on `firebase emulators:start` |
| T-2-01-03 | Information Disclosure | `firestore.rules` deny-all stub | mitigate | Deny-all is the safe default; no path accidentally allows access until Plan 03 explicitly opens rules |
</threat_model>

<verification>
After both tasks:
1. `firebase --version` exits 0
2. `java -version` exits 0
3. `test -f firebase.json` exits 0
4. `grep -c "collectionGroup" firestore.indexes.json` outputs 6
5. `grep "rules_version" firestore.rules` outputs `rules_version = '2';`
6. `test -f my-app/vitest.config.ts` exits 0
7. `grep "NEXT_PUBLIC_FIREBASE_API_KEY" my-app/.env.local.example` outputs the key name
8. `cat my-app/package.json | grep -c '"dev"'` outputs 1 (dev script updated)
</verification>

<success_criteria>
- Java 21 installed and in PATH
- firebase-tools installed globally
- my-app/package.json contains firebase@^12, firebase-admin@^14, server-only, @firebase/rules-unit-testing@^5, vitest, concurrently, tsx
- firebase.json at repo root with correct emulator ports (9099, 8080, 9199)
- firestore.rules exists with `rules_version = '2'` and deny-all stub
- firestore.indexes.json has exactly 6 composite index entries covering slots (3) and bookings (3)
- storage.rules exists
- .firebaserc exists with demo-easyappointment default
- my-app/vitest.config.ts exists pointing at lib/firebase/__tests__
- my-app/.env.local.example documents all 9 env vars
- my-app/package.json scripts include: dev (concurrently), dev:next, emulators, seed, test:rules
</success_criteria>

<output>
After completion, create `.planning/phases/02-firebase-foundation/02-01-SUMMARY.md`
</output>
