---
phase: 01-package
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - appoinment-scheduler/packages/react-easy-appointments/tests/formatSlotTime.test.ts
  - appoinment-scheduler/packages/react-easy-appointments/tests/deriveDate.test.ts
  - appoinment-scheduler/packages/react-easy-appointments/tests/dateRange.test.tsx
  - appoinment-scheduler/packages/react-easy-appointments/tests/ssr.test.tsx
autonomous: true
requirements: [PKG-01, PKG-02, PKG-05]
user_setup: []

must_haves:
  truths:
    - "New test files exist and FAIL (red) because the implementation does not exist yet"
    - "Test suite runs and reports the new failing tests without crashing the runner"
  artifacts:
    - path: "appoinment-scheduler/packages/react-easy-appointments/tests/formatSlotTime.test.ts"
      provides: "Failing tests for SSR-safe UTC time formatter (PKG-01, PKG-05)"
      contains: "formatSlotTime"
    - path: "appoinment-scheduler/packages/react-easy-appointments/tests/deriveDate.test.ts"
      provides: "Failing tests for UTC date-key extraction (PKG-01)"
      contains: "deriveDate"
    - path: "appoinment-scheduler/packages/react-easy-appointments/tests/dateRange.test.tsx"
      provides: "Failing tests for startDate/endDate props, toolbar disable, out-of-range greying (PKG-02)"
      contains: "out-of-range"
    - path: "appoinment-scheduler/packages/react-easy-appointments/tests/ssr.test.tsx"
      provides: "Failing test for theme=auto SSR-safe render (PKG-05)"
      contains: "theme"
  key_links:
    - from: "tests/formatSlotTime.test.ts"
      to: "src/utils/formatSlotTime.ts"
      via: "import { formatSlotTime } from '../src/utils/formatSlotTime'"
      pattern: "from '.*formatSlotTime'"
    - from: "tests/deriveDate.test.ts"
      to: "src/utils/deriveDate.ts"
      via: "import { deriveDate } from '../src/utils/deriveDate'"
      pattern: "from '.*deriveDate'"
---

<objective>
Create the failing test files (RED phase) for the new utilities and behaviors introduced in Phase 1. These tests define the contracts that Plans 01, 02, and 03 implement. The tests import modules that do not exist yet, so they MUST fail — that is the expected and correct Wave 0 outcome.

Purpose: Lock the behavioral contract before implementation so executors in later waves build against a fixed target (Nyquist compliance — every later task has an automated verify that already exists).
Output: Four new test files under `tests/`. They will be red until Plans 01–03 land.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-package/01-CONTEXT.md
@.planning/phases/01-package/01-RESEARCH.md
@.planning/phases/01-package/01-VALIDATION.md

<interfaces>
<!-- Contracts the tests assert against. Implemented in Plans 01-03. -->

Target Slot type (after Plan 01):
```typescript
export type SlotStatus = 'available' | 'pending' | 'booked' | 'unavailable'
export type Slot = {
  id: string
  date: string      // UTC date key "2026-05-19", == startUtc.slice(0,10)
  startUtc: string  // "2026-05-19T09:00:00Z"
  endUtc: string    // "2026-05-19T10:00:00Z"
  status: SlotStatus
  bookedByLabel?: string
}
```

Target utilities (created in Plan 01):
```typescript
// src/utils/formatSlotTime.ts
export function formatSlotTime(utcIso: string, locale?: string): string
//   returns '' when typeof window === 'undefined' (server)
//   returns Intl.DateTimeFormat local time string in browser

// src/utils/deriveDate.ts
export function deriveDate(startUtc: string): string  // startUtc.slice(0, 10)
```

Target Calendar props (added in Plan 02):
```typescript
startDate?: string  // "2026-05-19" — optional
endDate?: string    // "2026-05-25" — optional
```
CSS classes added: `.rea-month-cell--out-of-range`, `.rea-slot--pending`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit test stubs for formatSlotTime and deriveDate</name>
  <files>appoinment-scheduler/packages/react-easy-appointments/tests/formatSlotTime.test.ts, appoinment-scheduler/packages/react-easy-appointments/tests/deriveDate.test.ts</files>
  <read_first>
    - appoinment-scheduler/packages/react-easy-appointments/tests/types.test.ts (existing test style — import paths, vitest usage)
    - appoinment-scheduler/packages/react-easy-appointments/tests/setup.ts (test setup, jsdom)
    - appoinment-scheduler/packages/react-easy-appointments/vitest.config.ts (environment config)
  </read_first>
  <action>
Create `tests/formatSlotTime.test.ts`:
- Import: `import { describe, it, expect, afterEach, vi } from 'vitest'` and `import { formatSlotTime } from '../src/utils/formatSlotTime'`.
- Test 1 "returns a non-empty local time string in jsdom (window defined)": call `formatSlotTime('2026-05-19T09:00:00Z', 'en-US')`, assert the result is a non-empty string and matches `/\d/` (contains a digit). Do NOT assert an exact hour — the hour depends on the test runner's timezone.
- Test 2 "returns empty string when window is undefined (server)": temporarily delete the global window via `vi.stubGlobal('window', undefined)`, call `formatSlotTime('2026-05-19T09:00:00Z', 'en-US')`, assert it equals `''`. Use `afterEach(() => vi.unstubAllGlobals())` to restore.
- Test 3 "uses the provided locale without throwing": call `formatSlotTime('2026-05-19T09:00:00Z', 'fr-FR')`, assert it is a string (no throw).

Create `tests/deriveDate.test.ts`:
- Import: `import { describe, it, expect } from 'vitest'` and `import { deriveDate } from '../src/utils/deriveDate'`.
- Test 1 "extracts the UTC date portion": `expect(deriveDate('2026-05-19T09:00:00Z')).toBe('2026-05-19')`.
- Test 2 "extracts UTC date for a late-night UTC slot (no local shift)": `expect(deriveDate('2026-05-19T23:30:00Z')).toBe('2026-05-19')` — proves it uses string slice, not a Date object that would shift by timezone.
- Test 3 "handles a midnight UTC timestamp": `expect(deriveDate('2026-12-31T00:00:00Z')).toBe('2026-12-31')`.

These tests MUST fail right now because `src/utils/formatSlotTime.ts` and `src/utils/deriveDate.ts` do not exist yet. That is correct.
  </action>
  <verify>
    <automated>cd appoinment-scheduler && pnpm --filter react-easy-appointments test --run tests/formatSlotTime.test.ts tests/deriveDate.test.ts 2>&1 | grep -q "Cannot find module"</automated>
  </verify>
  <acceptance_criteria>
    - File appoinment-scheduler/packages/react-easy-appointments/tests/formatSlotTime.test.ts exists
    - formatSlotTime.test.ts contains the string `vi.stubGlobal('window', undefined)`
    - formatSlotTime.test.ts contains `from '../src/utils/formatSlotTime'`
    - File appoinment-scheduler/packages/react-easy-appointments/tests/deriveDate.test.ts exists
    - deriveDate.test.ts contains `toBe('2026-05-19')` AND `'2026-05-19T23:30:00Z'`
    - Running the two test files reports failures referencing the missing src/utils modules (RED state confirmed)
  </acceptance_criteria>
  <done>Both unit test files exist, import the not-yet-created utilities, and fail because those modules do not exist.</done>
</task>

<task type="auto">
  <name>Task 2: Create component test stubs for date range and SSR</name>
  <files>appoinment-scheduler/packages/react-easy-appointments/tests/dateRange.test.tsx, appoinment-scheduler/packages/react-easy-appointments/tests/ssr.test.tsx</files>
  <read_first>
    - appoinment-scheduler/packages/react-easy-appointments/tests/MonthView.test.tsx (existing render pattern with Calendar compound component and slot fixtures)
    - appoinment-scheduler/packages/react-easy-appointments/tests/Toolbar.test.tsx (existing toolbar render + button query pattern)
    - appoinment-scheduler/packages/react-easy-appointments/tests/Calendar.test.tsx (existing Calendar render pattern)
    - appoinment-scheduler/packages/react-easy-appointments/src/components/Calendar/Calendar.tsx (current CalendarProps — confirms startDate/endDate not yet present)
  </read_first>
  <action>
Create `tests/dateRange.test.tsx`:
- Imports: `import { describe, it, expect } from 'vitest'`, `import { render, screen } from '@testing-library/react'`, `import { Calendar } from '../src'`, `import type { Slot } from '../src'`.
- Build a fixture slot array using the NEW shape: `const slots: Slot[] = [{ id: 's1', date: '2026-05-19', startUtc: '2026-05-19T09:00:00Z', endUtc: '2026-05-19T10:00:00Z', status: 'available' }]`.
- Test 1 "prev button is disabled when current view is at startDate boundary": render `<Calendar slots={slots} startDate="2026-05-01" endDate="2026-05-31"><Calendar.Toolbar /><Calendar.MonthView /></Calendar>`. Query the Previous button via `screen.getByLabelText('Previous')`. Assert `expect(prevBtn).toBeDisabled()` (May 2026 is the startDate month, so prev must be disabled).
- Test 2 "next button is disabled when current view is at endDate boundary": same render. Query `screen.getByLabelText('Next')`. Assert `expect(nextBtn).toBeDisabled()` (May 2026 is the endDate month).
- Test 3 "renders out-of-range cells with rea-month-cell--out-of-range class": render with `startDate="2026-05-10" endDate="2026-05-20"`. Use `container` from render. Assert `container.querySelectorAll('.rea-month-cell--out-of-range').length` is greater than 0 (days 1-9 and 21-31 plus adjacent-month padding are out of range).
- Test 4 "calendar initializes to startDate month, not today": render with `startDate="2026-05-01" endDate="2026-05-31"`. Assert the toolbar title text contains "May 2026" via `screen.getByText(/May 2026/)`.
- Test 5 "WeekView renders no slots for days outside the range": render `<Calendar slots={slots} defaultView="week" startDate="2026-05-19" endDate="2026-05-19"><Calendar.Toolbar /><Calendar.WeekView /></Calendar>` with a fixture slot whose `date` is OUTSIDE the single-day range (e.g. a slot on `2026-05-22`). Because the only in-range day is 2026-05-19 and the slot lives on 2026-05-22, assert that no slot button for that out-of-range slot is rendered — query `screen.queryByRole('button', { name: /s1/i })` (or by the slot id/aria-label) and assert it is `null`. This fails now because WeekView has no out-of-range guard yet.

Create `tests/ssr.test.tsx`:
- Imports: `import { describe, it, expect } from 'vitest'`, `import { render } from '@testing-library/react'`, `import { Calendar } from '../src'`, `import type { Slot } from '../src'`.
- Fixture: one slot with the new UTC shape (as above).
- Test 1 "renders without throwing when theme='auto'": render `<Calendar slots={slots} theme="auto"><Calendar.Toolbar /><Calendar.MonthView /></Calendar>` inside an `expect(() => render(...)).not.toThrow()`.
- Test 2 "renders without throwing when theme='auto' and window.matchMedia is undefined": before render, `const orig = window.matchMedia; (window as any).matchMedia = undefined;` then assert render does not throw; restore `window.matchMedia = orig` in a finally block. This simulates the SSR/older-jsdom environment where matchMedia is absent.

Test 4 of dateRange and both ssr tests depend on startDate/endDate props and the SSR refactor that do not exist yet — they MUST fail now. Test 3 depends on the out-of-range CSS class not yet emitted. Test 5 depends on the WeekView out-of-range slot suppression not yet implemented. That is correct RED state.
  </action>
  <verify>
    <automated>cd appoinment-scheduler && pnpm --filter react-easy-appointments test --run tests/dateRange.test.tsx tests/ssr.test.tsx 2>&1 | grep -E "failed|FAIL|Test Files" || echo "EXPECTED_RED"</automated>
  </verify>
  <acceptance_criteria>
    - File tests/dateRange.test.tsx exists and contains `startUtc: '2026-05-19T09:00:00Z'`
    - dateRange.test.tsx contains `rea-month-cell--out-of-range`
    - dateRange.test.tsx contains `getByLabelText('Previous')` AND `getByLabelText('Next')`
    - dateRange.test.tsx contains `startDate="2026-05-01"`
    - dateRange.test.tsx contains a WeekView out-of-range assertion (`defaultView="week"` and a queryBy* asserting the out-of-range slot button is null)
    - File tests/ssr.test.tsx exists and contains `theme="auto"`
    - ssr.test.tsx contains `matchMedia = undefined` (the absent-matchMedia simulation)
    - Running both files reports failing tests (RED state — props and behaviors not implemented yet)
  </acceptance_criteria>
  <done>Both component test files exist using the new Slot UTC shape, assert the date-range and SSR behaviors, and currently fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase modifies a client-side React calendar rendering library. There are no network calls, authentication, persistence, or server-side input handling in the test-stub work. No trust boundary is crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-00-01 | Tampering | Test fixtures | accept | Test-only code; fixtures are static literals with no external input. No mitigation needed. |

No high-severity threats. ASVS L1 network/auth/data controls do not apply to a test-stub task in a UI package.
</threat_model>

<verification>
- `pnpm --filter react-easy-appointments test --run` executes without the runner crashing.
- The four new test files appear in the run output as failing (red), referencing missing utilities / unimplemented props.
- No existing passing test is broken by adding these files (they are additive).
</verification>

<success_criteria>
- Four new test files exist under `tests/`.
- All new tests are RED (failing) because the implementation does not exist yet.
- Test runner still completes (does not hang or crash on collection).
</success_criteria>

<output>
After completion, create `.planning/phases/01-package/01-00-SUMMARY.md`
</output>
